# Frontend environment. Copy to .env and fill in.
#
# WARNING: every VITE_ variable is compiled into the browser bundle and is
# readable by anyone who opens the site. Never put the Supabase service-role
# key or the OpenAI key here.

VITE_API_URL=http://localhost:3000
